#include "main.h"

/**
 * _abs - computes the absolute value of an intege
 * @n: n is an integer
 *
 * Return: integer
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n < 0)
	{
		return (-n);
	}
	esle
	{
		return (n);
	}
}
