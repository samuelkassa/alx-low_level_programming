#include <stdio.h>
#include "main.h"

/**
 * rev_string - reverses a string
 * @s: pointer variable
 *
 * Return: void
 */

void ret_string(char *s)
{
int j = 0;

while (s[j] != '\0')
{
j++;
}
j--;
while(s[j] >= 0)
{
printf("%s", s[j);
j--;
}
printf("\n");
}
